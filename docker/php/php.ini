[PHP]
memory_limit = 512M
max_execution_time = 120
max_input_time = 120

[curl]
; SSL certificates are provided by the ca-certificates package in the container
curl.cainfo = /etc/ssl/certs/ca-certificates.crt

[openssl]
openssl.cafile = /etc/ssl/certs/ca-certificates.crt
